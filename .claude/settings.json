{
  "permissions": {
    "allow": [
      "Bash(git:*)",
      "Bash(npm run:*)",
      "Bash(curl -s*github.io*)",
      "Bash(./scripts/autopush.sh *)",      // 允许直接执行
      "Bash(bash scripts/autopush.sh *)",   // 允许通过 bash 明确执行
      "Bash(scripts/autopush.sh *)",        // 允许相对路径执行
      "Edit(.claude/*)",
      "Write(.claude/*)",
      "Skill(update-config)",
      "Skill(update-config:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash scripts/autopush.sh 2>&1 | head -10 || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}