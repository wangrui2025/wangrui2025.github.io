{
  "permissions": {
    "allow": [
      "Bash(git:*)",
      "Bash(npm run:*)",
      "Bash(cd {workspace} && *)",
      "Edit({workspace}/.claude/*)",
      "Write({workspace}/.claude/*)",
      "Skill(update-config)",
      "Skill(update-config:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cd {workspace} && bash scripts/autopush.sh 2>&1 | head -10 || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
